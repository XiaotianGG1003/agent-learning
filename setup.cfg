[metadata]
name = my_agents
version = 0.0.0
description = Editable install exposing my_agents as top-level packages for development

[options]
packages = find:
package_dir =
    = agent
include_package_data = True

[options.packages.find]
where = agent
exclude =
